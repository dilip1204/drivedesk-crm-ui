version: "3.8"

services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ui
    restart: unless-stopped
    ports:
      - "80:80"
    networks:
      - drivedesk-net

networks:
  drivedesk-net:
    external: true
